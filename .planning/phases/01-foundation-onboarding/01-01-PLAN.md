---
plan: "01-01"
phase: 1
title: "Test Scaffold — Vitest, Playwright, RLS helpers"
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - vitest.setup.ts
  - playwright.config.ts
  - tests/helpers/supabase-rls.ts
  - tests/helpers/factories.ts
autonomous: true
requirements:
  - AUTH-01
  - PLAT-04
must_haves:
  truths:
    - "Running pnpm test executes unit tests without error"
    - "Running pnpm test:e2e runs Playwright against localhost:3000"
    - "RLS helper can assert a user cannot read another household's data"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config with jsdom environment, path aliases, setup file"
      min_lines: 20
    - path: "playwright.config.ts"
      provides: "Playwright config targeting localhost:3000, Chromium + Firefox"
      min_lines: 25
    - path: "tests/helpers/supabase-rls.ts"
      provides: "assertRlsBlocks() helper that creates two users and verifies cross-household isolation"
      min_lines: 40
    - path: "tests/helpers/factories.ts"
      provides: "createTestHousehold() and createTestUser() factory helpers for unit tests"
      min_lines: 30
  key_links:
    - from: "vitest.config.ts"
      to: "vitest.setup.ts"
      via: "setupFiles config option"
      pattern: "setupFiles.*vitest.setup"
    - from: "tests/helpers/supabase-rls.ts"
      to: "lib/supabase/admin.ts"
      via: "service role client to create test users and bypass RLS for setup"
      pattern: "createAdminClient|service_role"
---

<objective>
Create the test infrastructure that all implementation plans run against. This plan runs in Wave 0 — before any production code — so every subsequent plan can verify its work with automated commands.

Purpose: Establish a zero-excuses baseline. No implementation plan ships without a passing automated verify command.
Output: vitest.config.ts, playwright.config.ts, RLS isolation helper, test factory helpers.
</objective>

<execution_context>
@/Users/alexandrapop/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexandrapop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-onboarding/01-RESEARCH.md
</context>

<tasks>

<task id="1" type="auto">
  <name>Vitest configuration and unit test setup</name>
  <files>
    vitest.config.ts
    vitest.setup.ts
  </files>
  <action>
    Create vitest.config.ts using defineConfig from vitest/config:
    - environment: "jsdom"
    - globals: true
    - setupFiles: ["./vitest.setup.ts"]
    - resolve.alias: { "@": path.resolve(__dirname, "./src") }
    - include: ["src/**/*.test.ts", "src/**/*.test.tsx", "tests/unit/**/*.test.ts"]
    - plugins: [react()] from @vitejs/plugin-react and vitePluginTsconfigPaths() from vite-tsconfig-paths

    Create vitest.setup.ts:
    - Import @testing-library/react cleanup (afterEach)
    - Set up global fetch mock using vi.stubGlobal if needed
    - Export a noop (file must exist for setupFiles to resolve)

    Do NOT use jest globals (describe/it/expect are provided by vitest globals: true).
    Do NOT add @testing-library/jest-dom — use standard vitest matchers.
  </action>
  <verify>
    <automated>pnpm exec vitest run --reporter=verbose 2>&1 | head -20</automated>
  </verify>
  <done>vitest runs without config errors; "No test files found" is acceptable at this stage — zero errors is the bar</done>
</task>

<task id="2" type="auto">
  <name>Playwright configuration and E2E scaffold</name>
  <files>
    playwright.config.ts
    tests/e2e/.gitkeep
    tests/helpers/supabase-rls.ts
    tests/helpers/factories.ts
  </files>
  <action>
    Create playwright.config.ts:
    - testDir: "./tests/e2e"
    - baseURL: process.env.PLAYWRIGHT_BASE_URL ?? "http://localhost:3000"
    - projects: Chromium and Firefox desktop viewports
    - reporter: [["html", { open: "never" }]]
    - use: { trace: "on-first-retry" }
    - webServer: { command: "pnpm dev", url: "http://localhost:3000", reuseExistingServer: true }

    Create tests/e2e/.gitkeep (empty file — placeholder so directory commits).

    Create tests/helpers/supabase-rls.ts:
    - Export async function assertRlsBlocks(tableName: string, householdIdToBlock: string, userClient: SupabaseClient): Promise<void>
    - The function performs a SELECT query on tableName filtering by the given householdIdToBlock using the userClient (who belongs to a DIFFERENT household)
    - Asserts that the result array is empty (RLS blocked the read) using expect(rows).toHaveLength(0)
    - If the query errors (RLS throws), treat that as passing too
    - Import types from @supabase/supabase-js
    - Add JSDoc explaining: "Pass a Supabase client authenticated as User A, and a household_id belonging to User B. Asserts User A cannot read User B's household data."

    Create tests/helpers/factories.ts:
    - Export interface TestHousehold { id: string; name: string }
    - Export interface TestUser { id: string; email: string; password: string }
    - Export function makeTestUser(overrides?: Partial<TestUser>): TestUser — generates unique email/password using Date.now() suffix, does NOT create in DB
    - Export function makeTestHousehold(overrides?: Partial<TestHousehold>): TestHousehold — generates name with timestamp suffix, does NOT create in DB
    - These are in-memory factories only; DB creation is done in test setup blocks using the Supabase admin client
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript compiles cleanly for all four scaffold files; playwright.config.ts resolves without import errors; supabase-rls.ts and factories.ts export their expected types</done>
</task>

</tasks>

<verification>
- pnpm exec vitest run exits with code 0 (no test files = acceptable, no config errors = required)
- pnpm exec tsc --noEmit passes
- tests/helpers/supabase-rls.ts exports assertRlsBlocks function
- playwright.config.ts references localhost:3000
</verification>

<success_criteria>
Wave 0 is complete when: vitest can be invoked without crashing, Playwright config is valid, and the RLS helper exists with correct TypeScript types. All subsequent plans can now end their verify blocks with real test commands.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-onboarding/01-01-SUMMARY.md` following the summary template.
</output>
