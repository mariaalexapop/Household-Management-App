---
phase: 04-tracker-modules
plan: 06
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - src/app/(app)/cars/page.tsx
  - src/app/(app)/cars/CarsClient.tsx
autonomous: true
requirements: [CAR-01, CAR-02, CAR-03, CAR-05, CAR-06]

must_haves:
  truths:
    - "User sees a list of their cars on /cars with make, model, year, plate, colour"
    - "User can add a new car via a dialog form"
    - "User can view the service history timeline for any car"
    - "User can add, edit, and delete service records"
    - "User can set and edit key date reminders (MOT, tax, service) with configurable days-before"
    - "User can edit and delete cars"
  artifacts:
    - path: "src/app/(app)/cars/page.tsx"
      provides: "Server Component that fetches cars + service records"
    - path: "src/app/(app)/cars/CarsClient.tsx"
      provides: "Client Component with car list, forms, service timeline"
  key_links:
    - from: "src/app/(app)/cars/CarsClient.tsx"
      to: "src/app/actions/cars.ts"
      via: "createCar, updateCar, deleteCar, createServiceRecord, updateServiceRecord, deleteServiceRecord"
      pattern: "createCar|updateCar|deleteCar|createServiceRecord"
    - from: "src/app/(app)/cars/page.tsx"
      to: "src/lib/db/schema.ts"
      via: "db.select from cars + serviceRecords"
      pattern: "db\\.select.*cars"
---

<objective>
Build the car maintenance UI: a /cars page with car list, add/edit/delete car dialogs, service history timeline per car, and key date reminder configuration.

Purpose: Delivers the user-facing car maintenance module. Users can manage their cars and service history.
Output: Server Component page + Client Component with full car CRUD UI.
</objective>

<execution_context>
@/Users/alexandrapop/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexandrapop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-tracker-modules/04-CONTEXT.md
@.planning/phases/04-tracker-modules/04-RESEARCH.md
@.planning/phases/04-tracker-modules/04-01-SUMMARY.md
@.planning/phases/04-tracker-modules/04-02-SUMMARY.md
@.planning/phases/04-tracker-modules/04-03-SUMMARY.md
@.planning/UI-SPEC.md

<interfaces>
From src/app/actions/cars.ts (Plan 03):
```typescript
export async function createCar(data: unknown): Promise<ActionResult<{ id: string }>>
export async function updateCar(data: unknown): Promise<ActionResult<{ id: string }>>
export async function deleteCar(data: unknown): Promise<ActionResult>
export async function createServiceRecord(data: unknown): Promise<ActionResult<{ id: string }>>
export async function updateServiceRecord(data: unknown): Promise<ActionResult<{ id: string }>>
export async function deleteServiceRecord(data: unknown): Promise<ActionResult>
```

From src/components/tracker/ReminderConfig.tsx (Plan 02):
```typescript
export function ReminderConfig({ label, value, onChange }: { label: string; value: number; onChange: (days: number) => void })
```

From src/lib/format.ts (Plan 02):
```typescript
export function formatCostFromCents(cents: number | null | undefined): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create car page Server Component</name>
  <files>src/app/(app)/cars/page.tsx</files>
  <action>
Create `src/app/(app)/cars/page.tsx` as a Server Component following the same pattern as `/calendar/page.tsx` and `/kids/page.tsx`:

1. Auth check: `createClient()`, `getUser()`, redirect to login if not authenticated.
2. Get householdId via `getMemberRow` query (same as calendar page).
3. Parallel data fetch via `Promise.all`:
   - All cars for the household: `db.select().from(cars).where(eq(cars.householdId, householdId))`
   - All service records for the household: `db.select().from(serviceRecords).where(eq(serviceRecords.householdId, householdId))` ordered by serviceDate desc
4. Serialize Date objects to ISO strings for client component props.
5. Render page layout:
   - Miro design header with orange accent strip (`border-t-4 border-[#ea580c]` on a top bar element).
   - Page title "Cars" in `font-display text-2xl font-semibold`.
   - "Add Car" button top-right (orange accent: `bg-[#ea580c] hover:bg-[#c2410c] text-white`).
   - Pass cars and service records data to `<CarsClient>`.

Export metadata: `{ title: 'Cars -- Kinship' }`.
  </action>
  <verify>
    <automated>cd /Users/alexandrapop/Warp_project/Household-Management-App && rtk npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Cars page fetches data server-side and renders CarsClient. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create CarsClient with car list, forms, and service timeline</name>
  <files>src/app/(app)/cars/CarsClient.tsx</files>
  <action>
Create `src/app/(app)/cars/CarsClient.tsx` as a 'use client' component with:

**Props:** `{ cars: SerializedCar[]; serviceRecords: SerializedServiceRecord[] }` where serialized types have string dates instead of Date objects.

**State:** selectedCarId (for viewing service history), showAddCar dialog, showEditCar dialog, showAddService dialog, showEditService dialog.

**Car List:**
- Grid layout: `grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3`.
- Each car as a `ring-miro rounded-xl` card showing: car icon (lucide Car), make + model (font-display heading), year, plate, colour swatch.
- Below car details: 3 key dates shown as badges (MOT, Tax, Service) with date and "X days reminder" label. Use date-fns `formatDistanceToNow` or simple date format.
- Card actions: Edit button, Delete button (with confirmation dialog), "View Service History" button.
- Empty state: "No cars added yet. Add your first car to start tracking."

**Add/Edit Car Dialog:**
- Use @base-ui/react/dialog (project pattern -- NOT Radix).
- react-hook-form + @hookform/resolvers/zod with the createCarSchema.
- Fields: make, model, year, plate, colour (text input with colour preview).
- Key dates section: 3 date pickers (MOT due, Tax due, Next service) using the existing DatePicker component.
- Reminder config section: 3 `<ReminderConfig>` components (MOT days, Tax days, Service days).
- Submit calls createCar or updateCar server action. Shows success/error toast via sonner.

**Service History Timeline:**
- Shown when a car is selected. Slides in or expands below the car card.
- Timeline list ordered newest first. Each entry as a card: date, service type badge (colour-coded), mileage, garage name, cost (formatted via `formatCostFromCents`), notes.
- "Add Service Record" button at top of timeline.
- Add/Edit Service Record dialog: date picker, service type select ('full_service', 'mot', 'repair', 'tyre', 'other'), mileage input, garage input, cost input (user enters pounds, convert to cents via `poundsToCents`), notes textarea.
- Delete service record with confirmation.

**Miro Design throughout:**
- `ring-miro rounded-xl` cards
- `font-display` for headings, `font-body` for body text
- Orange accent colour `#ea580c` for primary actions and badges
- `bg-kinship-surface` page background
  </action>
  <verify>
    <automated>cd /Users/alexandrapop/Warp_project/Household-Management-App && rtk npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>CarsClient renders car cards with CRUD, service history timeline, key date reminders, and Miro design. All interactions call server actions. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `rtk npx tsc --noEmit` passes
- /cars page renders car list
- Add/edit/delete car works via dialogs
- Service history shows per car
- Key date reminder config works
</verification>

<success_criteria>
Users can navigate to /cars, see their cars, add/edit/delete cars with key date reminders, and view/manage service history per car.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tracker-modules/04-06-SUMMARY.md`
</output>
