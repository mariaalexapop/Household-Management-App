---
phase: 04-tracker-modules
plan: 08
type: execute
wave: 3
depends_on: [04-02, 04-05]
files_modified:
  - src/app/(app)/electronics/page.tsx
  - src/app/(app)/electronics/ElectronicsClient.tsx
autonomous: true
requirements: [ELEC-01, ELEC-02, ELEC-03, ELEC-04, ELEC-06]

must_haves:
  truths:
    - "User sees a list of their electronics items on /electronics"
    - "User can add an item with name, brand, model number, purchase date, cost"
    - "User can set warranty expiry date and coverage summary"
    - "User can upload warranty documents and user manuals (PDF)"
    - "User can edit and delete items"
  artifacts:
    - path: "src/app/(app)/electronics/page.tsx"
      provides: "Server Component that fetches items + documents"
    - path: "src/app/(app)/electronics/ElectronicsClient.tsx"
      provides: "Client Component with item list, forms, document upload"
  key_links:
    - from: "src/app/(app)/electronics/ElectronicsClient.tsx"
      to: "src/app/actions/electronics.ts"
      via: "createItem, updateItem, deleteItem"
      pattern: "createItem|updateItem|deleteItem"
    - from: "src/app/(app)/electronics/ElectronicsClient.tsx"
      to: "src/components/tracker/FileUploadZone.tsx"
      via: "PDF upload for warranty + manual docs"
      pattern: "FileUploadZone"
---

<objective>
Build the electronics registry UI: a /electronics page with item list, add/edit/delete item dialogs, warranty and manual document uploads.

Purpose: Delivers the user-facing electronics module. Users can manage their appliance/electronics registry with warranty tracking and manual uploads.
Output: Server Component page + Client Component with full electronics CRUD UI including document uploads.
</objective>

<execution_context>
@/Users/alexandrapop/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexandrapop/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-tracker-modules/04-CONTEXT.md
@.planning/phases/04-tracker-modules/04-RESEARCH.md
@.planning/phases/04-tracker-modules/04-01-SUMMARY.md
@.planning/phases/04-tracker-modules/04-02-SUMMARY.md
@.planning/phases/04-tracker-modules/04-05-SUMMARY.md
@.planning/UI-SPEC.md

<interfaces>
From src/app/actions/electronics.ts (Plan 05):
```typescript
export async function createItem(data: unknown): Promise<ActionResult<{ id: string }>>
export async function updateItem(data: unknown): Promise<ActionResult<{ id: string }>>
export async function deleteItem(data: unknown): Promise<ActionResult>
```

From src/components/tracker/FileUploadZone.tsx (Plan 02):
```typescript
export function FileUploadZone({ module, entityId, documentType, onUploadComplete, disabled }: Props)
```

From src/components/tracker/DocumentList.tsx (Plan 02):
```typescript
export function DocumentList({ documents, onDelete }: Props)
```

From src/lib/format.ts (Plan 02):
```typescript
export function formatCostFromCents(cents: number | null | undefined): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create electronics page Server Component</name>
  <files>src/app/(app)/electronics/page.tsx</files>
  <action>
Create `src/app/(app)/electronics/page.tsx` following the same pattern as cars/page.tsx:

1. Auth check + household resolution.
2. Parallel data fetch: all electronics items + all documents where `module='electronics'`.
3. Serialize dates to ISO strings.
4. Page layout with teal accent strip (`border-t-4 border-[#0d9488]`), "Electronics" title, "Add Item" button (teal: `bg-[#0d9488] hover:bg-[#0f766e] text-white`).
5. Pass items and documents to `<ElectronicsClient>`.

Export metadata: `{ title: 'Electronics -- Kinship' }`.
  </action>
  <verify>
    <automated>cd /Users/alexandrapop/Warp_project/Household-Management-App && rtk npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Electronics page fetches items + documents server-side and renders ElectronicsClient. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create ElectronicsClient with item list, forms, and document uploads</name>
  <files>src/app/(app)/electronics/ElectronicsClient.tsx</files>
  <action>
Create `src/app/(app)/electronics/ElectronicsClient.tsx` as a 'use client' component:

**Props:** `{ items: SerializedElectronicsItem[]; documents: SerializedDocument[] }`

**Item List:**
- Grid layout: `grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3`.
- Each item as a `ring-miro rounded-xl` card showing:
  - Monitor icon (lucide Monitor or Smartphone)
  - Name (font-display heading)
  - Brand + model number
  - Purchase date + cost (formatted via `formatCostFromCents`)
  - Warranty status: if warrantyExpiryDate is set, show "Warranty expires in X days" with date-fns `formatDistanceToNow`. If expired, show "Warranty expired" in red. If no warranty, show "No warranty".
  - Coverage summary (truncated, expandable)
- Card actions: Edit, Delete (confirmation dialog), expand for documents section.
- Empty state: "No electronics items yet. Add your first item to start tracking."

**Add/Edit Item Dialog:**
- @base-ui/react/dialog pattern.
- react-hook-form + zod.
- Fields:
  - Item details: name, brand, model number.
  - Purchase: date (DatePicker), cost (user enters pounds -- convert to cents).
  - Warranty: expiry date (DatePicker), coverage summary (textarea). Note: "You'll receive a reminder 30 days before warranty expires" help text.
- Submit calls createItem or updateItem. Toast on success/error.

**Document Upload Section (per item):**
- Shown when item card is expanded.
- TWO upload zones (ELEC-02 + ELEC-04):
  - `<FileUploadZone module="electronics" entityId={item.id} documentType="warranty" onUploadComplete={...} />` with label "Warranty Document"
  - `<FileUploadZone module="electronics" entityId={item.id} documentType="manual" onUploadComplete={...} />` with label "User Manual"
- `<DocumentList documents={itemDocs} onDelete={...} />` showing all documents for this item.
- Filter documents by entityId from the full documents array.

**Miro Design throughout:**
- Teal accent `#0d9488` for primary actions
- `ring-miro rounded-xl` cards, `font-display` headings, `font-body` text
  </action>
  <verify>
    <automated>cd /Users/alexandrapop/Warp_project/Household-Management-App && rtk npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>ElectronicsClient renders item cards with CRUD, dual document upload (warranty + manual), warranty status display. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `rtk npx tsc --noEmit` passes
- /electronics page renders item list
- Add/edit/delete item works
- Warranty and manual PDF uploads work
- Documents show with download/delete
</verification>

<success_criteria>
Users can navigate to /electronics, manage their electronics registry, upload warranty docs and user manuals, and see warranty expiry status.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tracker-modules/04-08-SUMMARY.md`
</output>
